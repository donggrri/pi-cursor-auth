{
  "name": "pi-cursor-auth",
  "version": "0.2.0",
  "description": "pi extension: Cursor models through pi's harness (extensions and pi tools stay in the loop)",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package", "pi-extension", "cursor", "cursor-sdk", "ai"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@cursor/sdk": "1.0.27"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
